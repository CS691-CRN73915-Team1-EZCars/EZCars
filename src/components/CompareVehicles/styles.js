// Add this object at the top of CompareVehicles.jsx if you want basic styling.
const styles = {
    comparisonContainer: {
      display: "flex",
      gap: "20px",
      padding: "20px",
    },
    vehicleCard: {
      border: "1px solid #ddd",
      padding: "10px",
      borderRadius: "8px",
      backgroundColor: "#fff",
      boxShadow: "0 4px 8px rgba(0, 0, 0, 0.1)"
    },
  };
  
  // Then use `styles.comparisonContainer` and `styles.vehicleCard` as appropriate in the component JSX
  